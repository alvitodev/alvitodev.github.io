---
import Layout from '@/layouts/Layout.astro'
import Panel from '@/components/Panel.astro'
import { Image } from 'astro:assets'

// Data Skill (Bisa dipindah ke content collection nanti jika mau)
const skills = [
  { name: 'JavaScript / TypeScript', level: 90 },
  { name: 'React / Astro', level: 85 },
  { name: 'Node.js', level: 80 },
  { name: 'UI/UX Design', level: 75 },
  { name: 'DevOps / CI/CD', level: 60 },
]

// Data Experience (Mission Log)
const experience = [
  {
    role: 'FULLSTACK ENGINEER',
    company: 'FREELANCE',
    date: '2023 - PRESENT',
    desc: 'Mengembangkan aplikasi web modern menggunakan teknologi stack terkini (Astro, React, Tailwind). Fokus pada performa dan aksesibilitas.'
  },
  {
    role: 'INFORMATICS STUDENT',
    company: 'UNIVERSITAS NEGERI YOGYAKARTA',
    date: '2024 - PRESENT',
    desc: 'Mempelajari dasar-dasar ilmu komputer, algoritma, dan struktur data. Aktif dalam organisasi kemahasiswaan teknologi.'
  }
]
---

<Layout title="Personnel File // Alvito">
  <div class="max-w-6xl mx-auto p-4 md:p-8 min-h-screen">
    
    {/* Header Halaman */}
    <header class="mb-8 border-b border-zinc-800 pb-6">
      <div class="flex items-center gap-3 mb-2">
        <div class="w-3 h-3 bg-amber-500 rounded-sm"></div>
        <span class="text-xs font-mono text-amber-500 tracking-[0.2em]">CLASSIFIED // PERSONNEL_FILE</span>
      </div>
      <h1 class="text-4xl md:text-5xl font-black font-mono text-white tracking-tighter uppercase">
        OPERATOR_PROFILE
      </h1>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-12 gap-6 md:gap-8">
      
      {/* --- KOLOM KIRI (Profil & Skill) --- */}
      <div class="md:col-span-4 flex flex-col gap-6">
        
        {/* ID CARD PANEL */}
        <Panel title="// IDENTIFICATION">
          <div class="flex flex-col items-center text-center">
            {/* Foto Profil dengan Efek Hexagon/Kotak */}
            <div class="relative w-32 h-32 mb-4 border-2 border-zinc-700 bg-zinc-800 overflow-hidden group">
              <div class="absolute inset-0 bg-amber-500/10 opacity-0 group-hover:opacity-100 transition-opacity z-10 pointer-events-none mix-blend-overlay"></div>
              {/* Ganti src dengan foto profil Anda */}
              <img 
                src="/static/logo.png" 
                alt="Profile" 
                class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500"
              />
              {/* Dekorasi Sudut Foto */}
              <div class="absolute top-0 right-0 w-2 h-2 bg-amber-500"></div>
              <div class="absolute bottom-0 left-0 w-2 h-2 bg-zinc-500"></div>
            </div>

            <h2 class="text-2xl font-bold font-mono text-white tracking-tight">ALVITO</h2>
            <p class="text-xs font-mono text-amber-500 mt-1 mb-4">LVL. 20 // DEVELOPER</p>

            <div class="w-full space-y-2 text-left bg-zinc-950/50 p-4 border border-zinc-800 text-xs font-mono text-zinc-400">
              <div class="flex justify-between">
                <span>STATUS:</span>
                <span class="text-green-500">ACTIVE</span>
              </div>
              <div class="flex justify-between">
                <span>LOC:</span>
                <span>YOGYAKARTA, ID</span>
              </div>
              <div class="flex justify-between">
                <span>SPECIALTY:</span>
                <span>WEB_OPS</span>
              </div>
            </div>
          </div>
        </Panel>

        {/* SKILL MATRIX */}
        <Panel title="// SKILL_MATRIX">
          <div class="space-y-4">
            {skills.map(skill => (
              <div>
                <div class="flex justify-between text-[10px] font-mono text-zinc-500 mb-1">
                  <span>{skill.name}</span>
                  <span>{skill.level}%</span>
                </div>
                <div class="w-full h-1.5 bg-zinc-800 border border-zinc-700">
                  <div 
                    class="h-full bg-amber-500/80 relative" 
                    style={`width: ${skill.level}%`}
                  >
                    {/* Efek kilauan di ujung bar */}
                    <div class="absolute right-0 top-0 bottom-0 w-1 bg-white/50"></div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </Panel>

        {/* CONTACT */}
        <Panel title="// COMM_CHANNELS">
          <div class="flex flex-col gap-2">
            <a href="mailto:email@alvito.dev" class="flex items-center justify-between p-2 border border-zinc-800 hover:border-amber-500/50 hover:bg-zinc-900 transition-colors group">
              <span class="text-xs font-mono text-zinc-400 group-hover:text-white">ENCRYPTED_MAIL</span>
              <span class="text-[10px] text-amber-500 opacity-0 group-hover:opacity-100 transition-opacity">&gt;&gt; SEND</span>
            </a>
            <a href="https://github.com/alvitodev" target="_blank" class="flex items-center justify-between p-2 border border-zinc-800 hover:border-amber-500/50 hover:bg-zinc-900 transition-colors group">
              <span class="text-xs font-mono text-zinc-400 group-hover:text-white">GITHUB_REPO</span>
              <span class="text-[10px] text-amber-500 opacity-0 group-hover:opacity-100 transition-opacity">&gt;&gt; ACCESS</span>
            </a>
          </div>
        </Panel>

      </div>

      {/* --- KOLOM KANAN (Bio & Timeline) --- */}
      <div class="md:col-span-8 flex flex-col gap-6">
        
        {/* BIO / ABSTRACT */}
        <Panel title="// ABSTRACT">
          <div class="prose prose-invert prose-zinc max-w-none prose-p:text-sm prose-p:font-mono prose-p:text-zinc-400">
            <p>
              Halo, saya Alvito. Seorang pengembang perangkat lunak yang berfokus pada pembangunan antarmuka web yang fungsional dan estetis.
              Saya tertarik pada konsep desain Industrial Sci-Fi, Cyberpunk, dan sistem yang terorganisir dengan rapi.
            </p>
            <p>
              Saat ini saya sedang menempuh pendidikan di Universitas Negeri Yogyakarta dan terus mengeksplorasi teknologi web modern seperti Astro, React, dan Tailwind CSS.
              Tujuan utama saya adalah menciptakan pengalaman digital yang tidak hanya bekerja dengan baik, tetapi juga memiliki karakter yang kuat.
            </p>
          </div>
        </Panel>

        {/* MISSION LOG (TIMELINE) */}
        <Panel title="// MISSION_LOG">
          <div class="relative space-y-8 pl-2">
            
            {/* Garis vertikal timeline */}
            <div class="absolute top-2 left-[19px] bottom-2 w-[1px] bg-zinc-800 border-l border-dashed border-zinc-700"></div>

            {experience.map((exp) => (
              <div class="relative pl-12 group">
                {/* Titik Timeline */}
                <div class="absolute left-[14px] top-1.5 w-2.5 h-2.5 bg-zinc-950 border border-zinc-600 group-hover:border-amber-500 group-hover:bg-amber-500/20 transition-colors rounded-sm rotate-45"></div>
                
                <div class="flex flex-col sm:flex-row sm:items-baseline sm:justify-between mb-1">
                  <h3 class="text-white font-bold font-mono text-lg group-hover:text-amber-500 transition-colors">
                    {exp.role}
                  </h3>
                  <span class="text-xs font-mono text-zinc-500 border border-zinc-800 px-2 py-0.5 rounded bg-zinc-950">
                    {exp.date}
                  </span>
                </div>
                
                <span class="text-xs font-mono text-amber-500/80 mb-2 block tracking-wider">
                  AT {exp.company}
                </span>
                
                <p class="text-sm text-zinc-400 leading-relaxed border-l-2 border-zinc-800 pl-4 group-hover:border-amber-500/30 transition-colors">
                  {exp.desc}
                </p>
              </div>
            ))}

          </div>
        </Panel>

        {/* EQUIPMENT / TOOLS */}
        <Panel title="// STANDARD_ISSUE_GEAR">
          <div class="grid grid-cols-2 sm:grid-cols-4 gap-4">
            {['VS Code', 'Zed Editor', 'Figma', 'Git', 'Terminal', 'Node.js', 'Chrome', 'Spotify'].map(tool => (
              <div class="flex items-center gap-2 p-2 border border-zinc-800 bg-zinc-900/30">
                <div class="w-1 h-1 bg-amber-500"></div>
                <span class="text-xs font-mono text-zinc-300">{tool}</span>
              </div>
            ))}
          </div>
        </Panel>

      </div>

    </div>
  </div>
</Layout>